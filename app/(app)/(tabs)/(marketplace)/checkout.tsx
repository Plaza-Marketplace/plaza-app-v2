import { StyleSheet } from 'react-native';
import React from 'react';
import ConfirmCartScreen from '@/screens/Checkout/CheckoutCart';

const Checkout = () => {
  return <ConfirmCartScreen />;
};

export default Checkout;

const styles = StyleSheet.create({});
